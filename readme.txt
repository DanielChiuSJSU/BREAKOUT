1. find or create a .xsa file
2. in Vitis, create a New Application Project using the .xsa file as the platform
    - select the Hello World template
3. build the project
4. for first run, right-click breakout_system and select Run->Launch Hardware
    - NOTE: for subsequent runs, you can just hit the green play button at the top
